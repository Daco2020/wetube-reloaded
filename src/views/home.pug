extends base
include mixins/video

block content
    h1 Home!
    input(type="검색")
    button send

    ul 
        each info in videos 
            +video(info)
        else 
            li Sorry nothing found. 